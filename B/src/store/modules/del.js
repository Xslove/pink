// import router from '../../router/index.js'
const state={
    // list:[],
}
const mutations={
    // del(state,i){
    //     state.list.splice(i,1 )
    // }
}
export default{
    namespaced: true,
    state,
    mutations
}